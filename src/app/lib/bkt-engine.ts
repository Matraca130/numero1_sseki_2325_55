// @deprecated v4.4.5 — Dead code. 0 importers. PATH B migration complete.
// BKT computation now runs server-side in backend batch-review.ts.
// Safe to delete in next cleanup PR.

/** @deprecated Backend computes BKT v4 Recovery. */
export function updateBKT(): never {
  throw new Error('[bkt-engine] DEPRECATED: Backend computes BKT v4 via PATH B');
}
