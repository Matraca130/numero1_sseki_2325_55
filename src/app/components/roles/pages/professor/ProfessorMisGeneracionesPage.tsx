// ============================================================
// Axon — Professor: Mis generaciones (PLACEHOLDER for M4)
//
// This file exists ONLY so the `mis-generaciones` route resolves
// in M3's PR. The real implementation (Realtime subscription on
// `atlas.runs`, run history list) lives in M4 and will overwrite
// this file in its PR.
//
// Do NOT add UI here. M4 owns it.
// ============================================================
import React from 'react';

export function ProfessorMisGeneracionesPage() {
  return (
    <div className="p-6 text-sm text-muted-foreground">
      Pendiente — M4
    </div>
  );
}
