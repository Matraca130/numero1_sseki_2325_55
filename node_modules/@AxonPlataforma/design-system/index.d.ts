/**
 * @AxonPlataforma/design-system — TypeScript Declarations
 */

// ── Brand ──
export interface LogoSize {
  svg: string;
  text: string;
  gap: string;
}

export interface LogoTheme {
  svgColor: string;
  textColor: string;
}

export interface Brand {
  name: string;
  fullName: string;
  version: string;
  tagline: string;
  logo: {
    sizes: Record<'xs' | 'sm' | 'md' | 'lg' | 'watermark', LogoSize>;
    themes: Record<'light' | 'dark' | 'gradient', LogoTheme>;
  };
}

export declare const brand: Brand;

// ── Colors ──
export interface ColorScale {
  50: string;
  100: string;
  200: string;
  300: string;
  400: string;
  500: string;
  600: string;
  700: string;
  800: string;
  900: string;
}

export interface Colors {
  primary: ColorScale;
  dark: {
    navBar: string;
    sidebarBase: string;
    sidebarBody: string;
    cardDark: string;
  };
  surface: {
    page: string;
    dashboard: string;
    card: string;
    hover: string;
  };
  border: {
    card: string;
    subtle: string;
    dark: string;
    darkAlt: string;
  };
  text: {
    primary: string;
    secondary: string;
    tertiary: string;
    disabled: string;
    inverse: string;
  };
  semantic: {
    success: string;
    warning: string;
    error: string;
    info: string;
  };
  mastery: {
    notStarted: string;
    learning: string;
    reviewing: string;
    mastered: string;
  };
  ratings: Record<1 | 2 | 3 | 4 | 5, string>;
  courseAccents: Record<string, { bg: string; text: string }>;
  chart: {
    flashcards: string;
    videos: string;
    bar: string;
    grid: string;
  };
}

export declare const colors: Colors;

// ── Typography ──
export interface TypographyRule {
  family: string;
  style: string;
  size: string;
  weight: string;
  tracking?: string;
  extra?: string;
}

export interface Typography {
  families: {
    heading: string;
    body: string;
    display: string;
    mono: string;
  };
  imports: string[];
  cssVariable: string;
  rules: Record<'pageTitle' | 'sectionTitle' | 'sectionLabel' | 'cardTitle' | 'body' | 'caption' | 'label', TypographyRule>;
}

export declare const typography: Typography;

// ── Shapes ──
export interface Shapes {
  card: string;
  button: {
    pill: string;
    action: string;
    filter: string;
    filterAlt: string;
  };
  icon: string;
  avatar: string;
  progress: string;
  input: string;
  badge: string;
  sidebar: {
    navItem: string;
    dropdown: string;
  };
}

export declare const shapes: Shapes;

// ── Shadows ──
export interface Shadows {
  card: string;
  cardHover: string;
  chart: string;
  performance: string;
  dropdown: string;
  tooltip: string;
}

export declare const shadows: Shadows;

// ── Components ──
export interface Components {
  icon: {
    default: { bg: string; text: string };
    sizes: Record<'sm' | 'md' | 'lg', string>;
    container: string;
  };
  card: {
    base: string;
    hover: string;
    padding: string;
    paddingLg: string;
    paddingXl: string;
  };
  cardDark: {
    base: string;
    padding: string;
  };
  buttonPrimary: {
    base: string;
    sizes: Record<'sm' | 'md' | 'lg', string>;
  };
  buttonAction: {
    base: string;
    full: string;
  };
  filterButton: {
    active: string;
    inactive: string;
    activeDark: string;
    inactiveDark: string;
  };
  progressBar: {
    track: string;
    fill: string;
    colorDefault: string;
  };
  progressCircle: {
    strokeBg: string;
    strokeActive: string;
    strokeWidth: number;
    strokeWidthSmall: number;
    strokeLinecap: string;
    strokeColorSmall: string;
  };
  kpiCard: {
    base: string;
    iconBg: string;
    trend: { up: string; down: string };
  };
  pageHeader: {
    wrapper: string;
    title: string;
    titleFont: string;
    subtitle: string;
    subtitleFont: string;
  };
  sidebar: {
    width: number;
    bgOuter: string;
    bgInner: string;
    borderColor: string;
    navItem: {
      base: string;
      active: string;
      inactive: string;
    };
    sectionLabel: string;
  };
  header: {
    height: string;
    bg: string;
    border: string;
    menuBtn: string;
  };
  courseSwitcher: {
    iconSize: string;
    iconRadius: string;
    courseLabel: string;
    courseName: string;
    dropdown: string;
  };
  shortcutCard: {
    base: string;
    iconSize: string;
    iconBg: string;
    iconColor: string;
  };
  courseCard: {
    base: string;
    iconSize: string;
    ctaButton: string;
  };
  activityItem: {
    iconSize: string;
    layout: string;
  };
  chartCard: {
    base: string;
    tooltip: { borderRadius: string; border: string; boxShadow: string };
  };
}

export declare const components: Components;

// ── Section Colors ──
export interface SectionColorEntry {
  bg: string;
  light: string;
  text: string;
  border: string;
  dot: string;
  badge?: string;
  glow?: string;
  ring?: string;
}

export interface DisciplineColor {
  icon: string;
  iconBg: string;
  progressColor: string;
  percentColor: string;
}

export interface SectionColors {
  teal: SectionColorEntry[];
  multi: SectionColorEntry[];
  disciplines: Record<string, DisciplineColor>;
}

export declare const sectionColors: SectionColors;

// ── Navigation ──
export interface NavItem {
  id: string;
  label: string;
  icon: string;
  view?: string;
  subtitle?: string;
  title?: string;
}

export interface Navigation {
  views: string[];
  primaryItems: NavItem[];
  secondaryItems: NavItem[];
  shortcuts: NavItem[];
}

export declare const navigation: Navigation;

// ── Layout ──
export interface Layout {
  sidebar: { width: number; collapsedWidth: number };
  header: { height: number };
  content: { paddingX: string; paddingY: string; gap: string; maxWidth: string };
  grid: Record<string, string>;
  rightPanel: { width: string };
}

export declare const layout: Layout;

// ── Animation ──
export interface AnimationPreset {
  initial: Record<string, number>;
  animate?: Record<string, number>;
  exit?: Record<string, number>;
  duration?: number;
  staggerDelay?: number;
  collapsed?: Record<string, number>;
  ease?: string;
}

export interface Animation {
  pageTransition: AnimationPreset;
  cardEntrance: AnimationPreset;
  progressBar: AnimationPreset;
  sidebar: AnimationPreset;
  dropdown: AnimationPreset;
}

export declare const animation: Animation;

// ── Design Rules ──
export interface DesignRules {
  mandatory: string[];
  forbidden: string[];
  fileStructure: Record<string, string>;
}

export declare const designRules: DesignRules;

// ── Backward compatibility ──
/** @deprecated Use `brand` instead */
export declare const BrandInfo: { name: string; version: string };
