---
name: Keep state fresh — volatile info in memory only
description: git pull before working (in CLAUDE.md). Volatile state (bugs, branches, tech debt) goes in memory, NOT in CLAUDE.md.
type: feedback
---

After completing significant work, update `project_current_state.md` with what changed.
NEVER put volatile state in CLAUDE.md — it is for stable architecture/commands/conventions only.
