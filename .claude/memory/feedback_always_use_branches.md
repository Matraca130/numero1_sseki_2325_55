---
name: Always use branches and PRs
description: Never push to main (also in CLAUDE.md). gh CLI not authenticated — push branch, user merges via browser.
type: feedback
---

`gh` CLI is not authenticated. Push feature branches and tell user to create PR via browser.
If work accidentally committed to main: create branch from it and reset main.
